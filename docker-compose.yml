services:
  server:
    build: ./server
    ports:
      - "3000:3000"
    env_file:
      - path: ./server/.env
        required: false
    environment:
      - DB_PATH=/data/claw-chat.db
      - PORT=3000
    volumes:
      - db-data:/data
    restart: unless-stopped

volumes:
  db-data:
